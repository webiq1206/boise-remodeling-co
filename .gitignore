# dependencies
node_modules

# next.js
.next
out

# caches
.npm-cache
.cache
test-results
playwright-report

# env files
.env
.env.local
.env.production

# assets not meant for git
attached_assets
.uploads
public/images/blog/Screenshot_*.png
tsconfig.tsbuildinfo

# Machine-local Claude Code settings (tool permissions). Specific to one
# developer's machine, so it must not follow the repo to Replit or a teammate.
.claude/settings.local.json

# Estimator test outputs
/p5-verification/
