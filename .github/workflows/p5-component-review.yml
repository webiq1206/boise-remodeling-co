name: P5 responsive components and visual review
on:
  push:
    branches: [codex/p5-visual-review-20260910, codex/p5-visual-final-20260910, codex/p5-complete-main-20260910, codex/p5-audit-complete-20260910]
    paths-ignore: [docs/**]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: p5-components-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    container: mcr.microsoft.com/playwright:v1.60.0-noble
    env:
      TZ: UTC
      SESSION_SECRET: p5-visual-review-ephemeral-session-key-20260910
      P5_SITE: remodeling
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm ci --no-audit --no-fund
      - name: Add temporary component fixture
        run: |
          mkdir -p app/p5-audit-fixture
          cat > app/p5-audit-fixture/page.tsx <<'FIXTURE'
          "use client";
          import { EstimatePromptBand } from "@/components/marketing/EstimatePromptBand";
          import { useEffect, useState } from "react";
          import { NotificationsBell } from "@/components/NotificationsBell";
          import { BeforeAfterSlider } from "@/components/BeforeAfterSlider";
          export default function AuditFixture() {
            const [ready, setReady] = useState(false);
            useEffect(() => setReady(true), []);
            return <main data-audit-ready={ready} className="container py-12"><h1>Component verification</h1><div id="notifications-fixture"><NotificationsBell /></div><BeforeAfterSlider beforeSrc="/p5-audit-before.svg" afterSrc="/p5-audit-after.svg" beforeAlt="Test rectangle before" afterAlt="Test rectangle after" /><div id="single-card" className="ed-cards-3 my-8"><article className="p-8 border">One featured idea</article></div><div id="four-cards" className="ed-cards-3 mt-12">{[1,2,3,4].map(n=><article key={n} className="p-8 border">Card {n}</article>)}</div><EstimatePromptBand title="Planning details" description="Three useful planning points." /></main>;
          }
          FIXTURE
          printf '<svg xmlns="http://www.w3.org/2000/svg" width="800" height="600"><rect width="800" height="600" fill="gray"/></svg>' > public/p5-audit-before.svg
          printf '<svg xmlns="http://www.w3.org/2000/svg" width="800" height="600"><rect width="800" height="600" fill="tan"/></svg>' > public/p5-audit-after.svg
      - name: Production build and repository gates
        id: production_build
        run: npm run build
      - name: Start the production server
        run: |
          npx next start -p 5000 > /tmp/p5-component-server.log 2>&1 &
          node -e 'let n=0;const t=setInterval(async()=>{try{const r=await fetch("http://127.0.0.1:5000");if(r.ok){clearInterval(t);return}}catch{}if(++n>90){clearInterval(t);process.exit(1)}},1000)'
      - name: Check all shared components at nine widths
        run: node scripts/p5-component-review.mjs
      - name: Check assistant and form separation
        if: always() && steps.production_build.outcome == 'success'
        run: node scripts/p5-launcher-forms.mjs
      - name: Verify mobile wizard navigation and scope intake
        run: P5_PARENT=0 node scripts/p5-mobile-navigation.mjs
      - name: Verify preserved estimator workflow in the merged build
        run: node scripts/p5-estimator-browser.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: p5-component-review
          path: |
            p5-component-review
            p5-verification
            /tmp/p5-component-server.log
          retention-days: 14
      - name: Upload compact diagnostics
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: p5-diagnostics-components
          path: |
            p5-visual-review/*.json
            p5-component-review/*.json
            p5-verification/*.json
            /tmp/p5-review-summary.json
            /tmp/p5-review-server.log
            /tmp/p5-component-server.log
          retention-days: 14
